# ValutaTrade Hub

Консольное приложение для торговли валютами и криптовалютами.
Проект реализован в рамках учебного задания и демонстрирует
работу с CLI, бизнес-логикой, внешними API и кешированием данных.

---

## 📌 Возможности приложения

- Регистрация и авторизация пользователей
- Управление портфелем валют
- Покупка и продажа валют
- Получение курсов валют
- Обновление курсов из внешних API
- Кеширование курсов с TTL
- Консольный интерфейс (CLI)

---

## 🛠️ Технологии

- Python 3.10+
- Poetry — управление зависимостями
- PrettyTable — вывод таблиц в CLI
- CoinGecko API — курсы криптовалют
- ExchangeRate API — курсы фиатных валют
- JSON — хранение данных

---

## 📂 Структура проекта

valutatrade_hub/
 core/ # Бизнес-логика
 cli/ # CLI интерфейс
 infra/ # Работа с данными
 parser_service/ # Сервис получения курсов
 data/ # JSON-хранилище
 logs/ # Логи приложения


## ⚙️ Установка и запуск

### 1. Установка зависимостей

poetry install
poetry run project

📖 Основные команды

Пользователь

poetry run project register --username alice --password 1234
poetry run project login --username alice --password 1234
poetry run project logout
poetry run project whoami

Портфель

poetry run project portfolio
poetry run project add-money --amount 1000

Торговля

poetry run project buy --currency BTC --amount 0.01
poetry run project sell --currency BTC --amount 0.005

Курсы валют

poetry run project update-rates
poetry run project show-rates
poetry run project get-rate --from USD --to BTC

Внешние API
CoinGecko

Используется для получения курсов криптовалют.
Регистрация и API-ключ не требуются.

ExchangeRate API

Используется для получения курсов фиатных валют.
API-ключ задаётся через переменную окружения:

export EXCHANGERATE_API_KEY=your_api_key


Если ключ не задан, приложение продолжает работу,
используя доступные источники.

Примечания

Данные и состояние приложения сохраняются в JSON-файлах

Кеш курсов валют обновляется с учётом TTL

Логирование ведётся в файл logs/app.log
